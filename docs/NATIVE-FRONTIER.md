<!-- GENERATED by tests/native-frontier-test.coil. Do not edit. -->
# The open frontier

Every file in `repros/open/` is a bug this compiler still has, run through the real
pipeline at `main(7)`. Each has a GENUINELY FAILING test in
`tests/frontier/open-bugs-frontier.coil` -- run them with `coil test --suite frontier`,
which is red today and is supposed to be.

This table is regenerated and compared byte for byte on every everyday gate, so a bug
that is fixed, that regresses, or that merely changes how it is broken shows up as a
diff here rather than as a paragraph nobody rechecked.

| repro | status |
|---|---|
| `for-await-has-no-bridge-kind.js` | refused |
| `for-of-destructuring-pattern.js` | refused |
| `rest-parameters-are-unimplemented.js` | refused |
| `shortest-round-trip-digits.js` | disagrees |

`refused` means the frontend declines the program by name. `crashes` means it compiled
and the binary did not answer. `disagrees` means it compiled, ran, and computed something
other than what node computes -- the worst of the three, because only a differential
finds it. `agrees with node` must never appear: that is a fixed bug still filed as open,
and its test in the frontier suite has gone green.
