<!-- GENERATED by tools/status.mjs. Do not edit; run `node tools/status.mjs`. -->
# Status: what compiles, and where the code comes from

**done** means a program written in TypeScript compiles through a definition in `lib/`.
**hand-written** means it compiles, but through IR the frontend builds itself.
Everything under "Not supported" does not compile at all — the call or the syntax is refused.

This file is generated and `tools/gate.sh` fails if it is out of date. The method names are
read out of the frontend's own tables, and every definition named here is checked to exist in
`lib/` and to be referenced from `src/`, so it cannot claim a conversion that is not wired up.

## String.prototype

| Operation | Status | Definition |
|---|---|---|
| `at` | **done** | StringAt |
| `charAt` | **done** | StringCharAt |
| `charCodeAt` | **done** | StringCharCodeAt |
| `codePointAt` | **done** | StringCodePointAt |
| `endsWith` | **done** | StringEndsWith |
| `includes` | **done** | StringIncludesFrom |
| `indexOf` | **done** | StringIndexOfFrom |
| `lastIndexOf` | **done** | StringLastIndexOf |
| `padEnd` | **done** | StringPadEnd |
| `padStart` | **done** | StringPadStart |
| `repeat` | **done** | StringRepeatCount |
| `replaceAll` | **done** | StringReplaceAll |
| `slice` | **done** | StringSlice |
| `split` | **done** | StringSplit |
| `startsWith` | **done** | StringStartsWith |
| `substr` | **done** | StringSubstr |
| `substring` | **done** | StringSubstring |
| `toLowerCase` | **done** | StringToLowerCase |
| `toUpperCase` | **done** | StringToUpperCase |
| `trim` | **done** | StringTrim |
| `trimEnd` | **done** | StringTrimEnd |
| `trimStart` | **done** | StringTrimStart |

## Array.prototype

| Operation | Status | Definition |
|---|---|---|
| `at` | **done** | ArrayAt |
| `concat` | **done** | ArrayConcatValue |
| `every` | **done** | ArrayEvery |
| `fill` | **done** | ArrayFill |
| `filter` | **done** | ArrayFilter |
| `find` | **done** | ArrayFind |
| `findIndex` | **done** | ArrayFindIndex |
| `flat` | **done** | ArrayFlat |
| `flatMap` | **done** | ArrayFlatMap |
| `forEach` | **done** | ArrayForEach |
| `includes` | **done** | ArrayIncludes |
| `indexOf` | **done** | ArrayIndexOfFrom |
| `join` | **done** | ArrayJoin |
| `lastIndexOf` | **done** | ArrayLastIndexOf |
| `map` | **done** | ArrayMap |
| `pop` | **done** | ArrayPop |
| `push` | **done** | ArrayPush1 |
| `reduce` | **done** | ArrayReduce |
| `reduceRight` | **done** | ArrayReduceRight |
| `reverse` | **done** | ArrayReverse |
| `shift` | **done** | ArrayShift |
| `slice` | **done** | ArraySlice |
| `some` | **done** | ArraySome |
| `sort` | **done** | ArraySort |
| `splice` | **done** | ArraySpliceDelete |
| `unshift` | **done** | ArrayUnshift1 |

## Math

| Operation | Status | Definition |
|---|---|---|
| `abs` | **done** | MathAbs |
| `acos` | hand-written | — |
| `asin` | hand-written | — |
| `atan` | hand-written | — |
| `ceil` | **done** | MathCeil |
| `cos` | hand-written | — |
| `exp` | hand-written | — |
| `floor` | **done** | MathFloor |
| `log` | hand-written | — |
| `max` | **done** | MathMax2 |
| `min` | **done** | MathMin2 |
| `pow` | hand-written | — |
| `random` | hand-written | — |
| `round` | **done** | MathRound |
| `sign` | **done** | MathSign |
| `sin` | hand-written | — |
| `sqrt` | hand-written | — |
| `tan` | hand-written | — |
| `trunc` | **done** | MathTrunc |

The hand-written rows are libm calls and a PRNG, not JavaScript semantics: there is no
algorithm for a DSL to express. `MathFloor` earns its place because the library adds the
`if (%IsInt v)` guard around `%FloorNum`; `sqrt` would get no guard and no rule, only a different
spelling of the same call. **These are not a backlog.**

## Globals, coercions and operators

| Operation | Status | Definition |
|---|---|---|
| `String(x)` | **done** | ToStringValue |
| `parseInt(s, radix)` | **done** | ParseIntValue |
| `isNaN(x)` | **done** | GlobalIsNaN |
| `String.fromCharCode(c)` | **done** | StringFromCharCode |
| `Number(x)` | **done** | ToNumberValue |
| `Number.isNaN` | **done** | NumberIsNaN |
| `Number.isFinite` | **done** | NumberIsFinite |
| `Number.isInteger` | **done** | NumberIsInteger |
| `Number.isSafeInteger` | **done** | NumberIsSafeInteger |
| ToNumber at an arithmetic operand | **done** | ToNumberValue |
| implicit ToString at `+` | **done** | ToStringValue |
| string `===` / `!==` | **done** | StringEquals |
| string `<` `>` `<=` `>=` | **done** | StringCompare |
| string `+` | **done** | StringConcat |
| `s.length` | **done** | StringLength |
| `a.length` | **done** | ArrayLength |
| `Array.of(...)` | **done** | ArrayOfAppend1 |
| `Array.isArray(x)` | **done** | IsArray |
| `Array.from(iterable)` | **done** | ArrayFrom |
| `Infinity` | **done** | Infinity |
| `Number.MAX_SAFE_INTEGER` | **done** | MaxSafeInteger |
| `parseFloat(x)` | **done** | ParseFloatValue |
| `Number.parseFloat(x)` | **done** | ParseFloatValue |
| `void x` | **done** | VoidValue |
| `typeof x` | **done** | TypeOfValue |
| `x ?? y` | **done** | IsNullishValue |
| `o?.p` | **done** | IsNullishValue |

## Language surface that compiles

Numbers and all 25 arithmetic, bitwise, comparison and logical operators including their
compound-assignment forms; `if`, `for`, `while`, `do`, `switch`, labels, `break`, `continue`,
ternaries and short-circuit operators; function declarations and expressions, closures with
mutable capture, recursion, `this`, `new` and constructors; object literals, structural types,
prototypes, dynamic and missing properties; array literals with holes, indexing and growth;
string literals and concatenation; `throw`; type aliases, interfaces and union annotations.

## Not supported

### Needs the allocating-definition shape `split` now has

- `Array.prototype.keys`/`values`/`entries`

### Needs a regular-expression engine

- regex literals
- `String.prototype.match`
- `String.prototype.matchAll`
- `String.prototype.search`
- `String.prototype.replace`
- `RegExp`

### Needs a frontend intrinsic, like the one `Number` just got

- `Object.keys`/`values`/`entries`
- `Object.assign`
- `Object.freeze`
- `JSON.parse`/`stringify`
- `String.prototype.normalize`
- `String.prototype.localeCompare`
- `String.raw`

### Needs new syntax in the frontend

- template literals
- destructuring
- spread and rest
- default parameters
- `for...of` / `for...in`
- `class`
- getters and setters
- computed property names
- optional calls/elements
- `try`/`catch`/`finally`
- arrow functions
- `instanceof` / `in` / `delete`

### Needs a runtime this compiler does not have

- `Map` / `Set` / `WeakMap`
- `Promise` and `async`/`await`
- generators and iterators
- `Symbol`
- `BigInt`
- `Proxy` / `Reflect`
- `Date`
- modules (`import`/`export`)

## Definitions in `lib/` that no program reaches

Each one is deliberate; a new name appearing here is a bug, and this tool fails on one.

- `ArrayIota`
- `ArrayMapDouble`
- `ArrayRepeat`
- `BoxedInf`
- `BoxedNegInf`
- `ClampedIndex`
- `FltCeilFrom`
- `FltFloorFrom`
- `FltTrunc`
- `Infinity`
- `IsNumber`
- `NegInfinity`
- `RequireObjectCoercible`
- `String.prototype.indexOf`
- `ToBoolean`
- `ToInt32`
- `ToLength`
- `ToUint32`

