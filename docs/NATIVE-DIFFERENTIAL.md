<!-- GENERATED by tests/native-differential-test.coil. Do not edit. -->
# Does it agree with node

Every method in `src/frontend_native_graph.coil`'s dispatch tables, compiled to arm64,
linked against `native/gc/runtime.c`, EXECUTED, and compared against the same source run
by node. `docs/NATIVE-CAPABILITY.md` says what compiles; this says what is right.

The probe hashes `String(r)` character by character rather than measuring its length,
because a length is blind to what these definitions decide -- changing `join`'s separator
from "," to ";" left a length-only test entirely green.

THE CALL IS DERIVED TOO. Each method is tried at the arity the frontend's own table
states, and the first argument list whose program BOTH compiles and runs under node is
the one recorded. A method with no such call is listed as `no runnable call`, which is a
statement about the probe rather than about the method.

| receiver | call | agrees with node |
|---|---|---|
| `"abcdef"` | `v.charAt(1)` | yes |
| `"abcdef"` | `v.charCodeAt(1)` | yes |
| `"abcdef"` | `v.codePointAt(1)` | yes |
| `"abcdef"` | `v.substring(1, 2)` | yes |
| `"abcdef"` | `v.substr(1, 2)` | yes |
| `"abcdef"` | `v.slice(1, 2)` | yes |
| `"abcdef"` | `v.split(1)` | yes |
| `"abcdef"` | `v.indexOf(1, 2)` | yes |
| `"abcdef"` | `v.toLowerCase()` | yes |
| `"abcdef"` | `v.toUpperCase()` | yes |
| `"abcdef"` | `v.startsWith(1, 2)` | yes |
| `"abcdef"` | `v.endsWith(1, 2)` | yes |
| `"abcdef"` | `v.includes(1, 2)` | yes |
| `"abcdef"` | `v.lastIndexOf(1, 2)` | yes |
| `"abcdef"` | `v.padStart(1, 2)` | yes |
| `"abcdef"` | `v.padEnd(1, 2)` | yes |
| `"abcdef"` | `v.repeat(1)` | yes |
| `"abcdef"` | `v.replaceAll(1, 2)` | yes |
| `"abcdef"` | `v.replace(1, 2)` | yes |
| `"abcdef"` | `v.trim()` | yes |
| `"abcdef"` | `v.trimStart()` | yes |
| `"abcdef"` | `v.trimEnd()` | yes |
| `"abcdef"` | `v.at(1)` | yes |
| `"abcdef"` | `v.localeCompare(1)` | yes |
| `[1, 2, 3]` | `v.concat("b")` | yes |
| `[1, 2, 3]` | `v.flat(1)` | yes |
| `[1, 2, 3]` | `v.splice(1)` | yes |
| `[1, 2, 3]` | `v.fill(1)` | yes |
| `[1, 2, 3]` | `v.reverse(1)` | yes |
| `[1, 2, 3]` | `v.unshift(1)` | yes |
| `[1, 2, 3]` | `v.push(1)` | yes |
| `[1, 2, 3]` | `v.pop(1)` | yes |
| `[1, 2, 3]` | `v.shift(1)` | yes |
| `[1, 2, 3]` | `v.slice(1)` | yes |
| `[1, 2, 3]` | `v.indexOf(1)` | yes |
| `[1, 2, 3]` | `v.includes(1)` | yes |
| `[1, 2, 3]` | `v.lastIndexOf(1)` | yes |
| `[1, 2, 3]` | `v.at(1)` | yes |
| `[1, 2, 3]` | `v.join(1)` | yes |
| `[1, 2, 3]` | `v.map(x => x)` | yes |
| `[1, 2, 3]` | `v.filter(x => x)` | yes |
| `[1, 2, 3]` | `v.forEach(x => x)` | yes |
| `[1, 2, 3]` | `v.reduce(x => x)` | yes |
| `[1, 2, 3]` | `v.reduceRight(x => x)` | yes |
| `[1, 2, 3]` | `v.find(x => x)` | yes |
| `[1, 2, 3]` | `v.findIndex(x => x)` | yes |
| `[1, 2, 3]` | `v.some(x => x)` | yes |
| `[1, 2, 3]` | `v.every(x => x)` | yes |
| `[1, 2, 3]` | `v.sort(x => x)` | **no** |
| `[1, 2, 3]` | `v.flatMap(x => x)` | yes |
| `[1, 2, 3]` | `v.keys(1)` | **no** |
| `[1, 2, 3]` | `v.values(1)` | **no** |
| `[1, 2, 3]` | `v.entries(1)` | **no** |

## Totals

| status | count |
|---|---|
| agrees with node | 49 |
| **disagrees** | 4 |
| no runnable call | 1 |
