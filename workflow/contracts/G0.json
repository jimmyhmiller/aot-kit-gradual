{
  "schemaVersion": 1,
  "milestone": "G0",
  "implementationComplete": true,
  "requiredLadderFixtures": 11,
  "note": "Set implementationComplete only after all G0 deliverables and exit gates are executable; the focused gate enforces this audit latch."
}
