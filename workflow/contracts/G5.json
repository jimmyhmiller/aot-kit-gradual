{
  "schemaVersion": 1,
  "milestone": "G5",
  "implementationComplete": true,
  "requiredAbiFixtures": 7,
  "requiredCorruptionWitnesses": 3,
  "note": "Closed by backend-abi/call tests: aligned local frames, FP/LR preservation, x0-x7 and stack args, caller-live preservation, deep/direct/mutual recursion, spill bounds, and named corruptions."
}
