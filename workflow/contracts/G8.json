{
  "schemaVersion": 1,
  "milestone": "G8",
  "implementationComplete": true,
  "requiredPlacementFixtures": 7,
  "requiredCorruptionWitnesses": 3,
  "note": "Close only after earliest/latest dominance placement, loop-depth choice, Phi-edge uses, pinned/effect constraints, memory anti-dependencies, deterministic dumps, and native differentials are executable."
}
