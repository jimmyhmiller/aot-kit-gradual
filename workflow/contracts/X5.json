{
  "schemaVersion": 1,
  "milestone": "X5",
  "implementationComplete": true,
  "requiredDepth": 21,
  "requiredModes": ["normal", "verify-heap", "register-pressure", "seed-matrix"],
  "requiredEvidence": [
    "exact-31-fields",
    "exact-allocation-count",
    "exact-allocation-bytes",
    "moving-collection",
    "multi-shape-layouts",
    "precise-stackmap-roots",
    "named-regression-witness"
  ],
  "note": "Added after X4 exposed invalid Tree.right=2 during relocation in the normalized TypeScript native path above depth 12. Close only after the root-location cause is fixed and depth 21 passes without enlarging or disabling the collector."
}
