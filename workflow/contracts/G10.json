{
  "schemaVersion": 1,
  "milestone": "G10",
  "implementationComplete": true,
  "requiredFixtures": 6,
  "requiredLayoutSeeds": 3,
  "requiredCorruptionWitnesses": 4,
  "note": "Close only after symbols, internal/external relocations, versioned stack/layout metadata, atomic validation, layout permutations, and external recursive execution are executable."
}
