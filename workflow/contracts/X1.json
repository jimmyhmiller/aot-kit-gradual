{
  "schemaVersion": 1,
  "milestone": "X1",
  "implementationComplete": true,
  "requiredFixtures": 10,
  "requiredStressDepth": 100,
  "requiredCorruptionWitnesses": 7,
  "note": "Close only after compiled fast/slow allocation, exact native frame/root relocation, descriptor scanning, recursion, promotion/barriers, configured OOM, and collect-every-allocation verification are executable."
}
