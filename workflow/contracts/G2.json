{
  "schemaVersion": 1,
  "milestone": "G2",
  "implementationComplete": true,
  "requiredSelectionFixtures": 6,
  "requiredCorruptionWitnesses": 3,
  "note": "Close only after selection consumes MBlocks, emits explicit terminators, and verifies ownership, completeness, and dependencies."
}
