#!/usr/bin/env bash
set -euo pipefail

for command in coil go node; do
  if ! command -v "$command" >/dev/null 2>&1; then
    printf 'error: %s is unavailable; rerun .agents/setup\n' "$command" >&2
    exit 1
  fi
done

printf 'Orb prerequisites are available; no services need resuming.\n'
