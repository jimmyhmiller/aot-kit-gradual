# V8 Benchmark Suite version 7 corpus

These files are unchanged upstream sources from V8 tag `7.4.77`, commit
`f96b55bd7c9c36e9ab5cbef08f094bf4c57f9707`. `manifest.json` records the canonical URLs and
SHA-256 hashes. The upstream BSD 3-Clause license is retained in `LICENSE`; source files also
retain their copyright and license headers.

Run `node tools/v8-fetch.mjs --verify` to verify the checked-in copy or
`node tools/v8-fetch.mjs --update` to restore it from the pinned upstream revision.
