;; Public backend facade. The implementation is layered behind this stable import so existing
;; tests, tools, and compiler entry points do not depend on internal phase boundaries.
(module backend)

(import "backend_core" :reexport)
(import "backend_cfg" :reexport)
(import "backend_select" :reexport)
(import "backend_liveness" :reexport)
(import "backend_schedule" :reexport)
(import "backend_allocate" :reexport)
(import "backend_aarch64" :reexport)
(import "backend_macho" :reexport)
